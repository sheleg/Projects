{ Определить лежит ли заданная точка слева или справа от прямой, которая
  задана своим направляющим вектором. }

program LineAndPoint;
uses Crt;
var
   lbx, lby: Real;  {Координаты начала направляющего вектора}
   lex, ley: Real;  {Координаты конца направляющего вектора}
   px, py: Real;    {Координаты точки}

{Функция вычисляет скалярное произведение вектора (lbx,lby)-(px,py) на
 вектор, перпендикулярный направляющему вектору прямой.}
function Otkl: Real;
var
  nx, ny: Real; {Координаты вектора, перпендикулярного направляющему вектору.}
begin
  nx := lby - ley;
  ny := lex - lbx;
  Otkl := nx * (px-lbx) + ny * (py - lby);
end;

Begin
  ClrScr;
  Write('Введите координаты начала направляющего вектора прямой: ');
  ReadLn(lbx, lby);
  Write('Введите координаты конца направляющего вектора прямой: ');
  ReadLn(lex, ley);
  Write('Введите координаты точки: ');
  ReadLn(px, py);
  if Otkl = 0 then
    WriteLn('Точка лежит на прямой')
  else
    if Otkl > 0 then
      WriteLn('Точка лежит слева от прямой')
    else
      WriteLn('Точка лежит справа от прямой');
End.